# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: CI - TEST
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm ci
        working-directory: ./assignment2

      - name: create dotenv
        run: |
          mkdir ./assignment2/functions
          touch ./assignment2/functions/.env
          echo "VUE_APP_API_KEY=${{ secrets.VUE_APP_API_KEY }}" >> ./assignment2/functions/.env
          echo "VUE_APP_AUTH_DOMAIN=${{ secrets.VUE_APP_AUTH_DOMAIN }}" >> ./assignment2/functions/.env
          echo "VUE_APP_DATABASE_URL=${{ secrets.VUE_APP_DATABASE_URL }}" >> ./assignment2/functions/.env
          echo "VUE_APP_PROJECT_ID=${{ secrets.VUE_APP_PROJECT_ID }}" >> ./assignment2/functions/.env
          echo "VUE_APP_STORAGE_BUCKET=${{ secrets.VUE_APP_STORAGE_BUCKET }}" >> ./assignment2/functions/.env
          echo "VUE_APP_APP_STORAGE_URL=${{ secrets.VUE_APP_APP_STORAGE_URL }}" >> ./assignment2/functions/.env
          echo "VUE_APP_MESSAGING_SENDER_ID=${{ secrets.VUE_APP_MESSAGING_SENDER_ID }}" >> ./assignment2/functions/.env
          echo "VUE_APP_APP_ID=${{ secrets.VUE_APP_APP_ID }}" >> ./assignment2/functions/.env
          echo "VUE_APP_APP_PW=${{ secrets.VUE_APP_APP_PW }}" >> ./assignment2/functions/.env
          cat ./assignment2/functions/.env

      - name: Build and deploy
        run: npm run build
        working-directory: ./assignment2

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_VUE2_CC0A4 }}"
          projectId: vue2-cc0a4
          channelId: live
