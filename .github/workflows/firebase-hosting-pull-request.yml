# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: CI - TEST
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm ci
        working-directory: ./assignment2

      - name: create dotenv
        run: cd ./assignment2
          ls
          touch .env
          echo VUE_APP_API_KEY=${{ secrets.VUE_APP_API_KEY }} >> .env
          echo VUE_APP_AUTH_DOMAIN=${{ secrets.VUE_APP_AUTH_DOMAIN }} >> .env
          echo VUE_APP_DATABASE_URL=${{ secrets.VUE_APP_DATABASE_URL }} >> .env
          echo VUE_APP_PROJECT_ID=${{ secrets.VUE_APP_PROJECT_ID }} >> .env
          echo VUE_APP_STORAGE_BUCKET=${{ secrets.VUE_APP_STORAGE_BUCKET }} >> .env
          echo VUE_APP_APP_STORAGE_URL=${{ secrets.VUE_APP_APP_STORAGE_URL }} >> .env
          echo VUE_APP_MESSAGING_SENDER_ID=${{ secrets.VUE_APP_MESSAGING_SENDER_ID }} >> .env
          echo VUE_APP_APP_ID=${{ secrets.VUE_APP_APP_ID }} >> .env
          echo VUE_APP_APP_PW=${{ secrets.VUE_APP_APP_PW }} >> .env
          cat .env

      - name: Build and deploy
        run: npm run build
        working-directory: ./assignment2

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_VUE2_CC0A4 }}"
          projectId: vue2-cc0a4
          channelId: live
